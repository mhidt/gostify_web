/**
 * Placeholder DOCX exporter.
 * Uses the `docx` package already in dependencies.
 */
export async function exportToDocx(markdown: string): Promise<void> {
  // TODO: implement real DOCX export using the `docx` package
  console.warn('exportToDocx: not yet implemented', markdown.length)
}
