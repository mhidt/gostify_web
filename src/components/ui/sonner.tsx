/* Minimal Toaster stub.
   Install `sonner` and replace with real component for production use. */

export function Toaster(_props: {
  richColors?: boolean
  position?: string
  [key: string]: unknown
}) {
  return null
}
